{$DOMAIN} {
	encode zstd gzip
	# 上传大小由 TripHub 按 .env 的 MAX_UPLOAD_MB 限制（超限返回 JSON 提示），这里不再重复限制
	# HSTS：浏览器记住本站只用 HTTPS 访问，防止在公共 Wi-Fi 下被降级成明文 HTTP
	header Strict-Transport-Security "max-age=31536000"
	reverse_proxy app:8080
}
